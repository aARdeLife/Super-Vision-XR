
// Constants and Variables
const video = document.getElementById('video');
let net;

// Utility functions
const setupWebcam = async () => {
    // ... (existing code)
};

const drawRect = (box, color) => {
    // ... (existing code)
};

const drawPredictions = async (predictions) => {
    // ... (existing code)
};

const detectFrame = async (video, net) => {
    // ... (existing code)
};

// App Initialization
const bindPage = async () => {
    // ... (existing code)
};

window.onload = bindPage;
